import AddPet from '../AddPet/AddPet';
import PetsList from '../PetsList/PetsList';

const PetsBlock = () => {
  return (
    <div>
      <AddPet />
      <PetsList />
    </div>
  );
};

export default PetsBlock;